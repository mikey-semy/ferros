# Артефакты сборки Rust и bootimage (всё кладётся в target/).
/target
# Артефакты сборки пользовательских крейтов (собираются из build.rs, M5c1).
/user/*/target

# Локальные файлы окружения/редактора.
*.log
.DS_Store
