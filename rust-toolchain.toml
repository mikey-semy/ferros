# Закрепляем nightly для всего проекта: для ОС нужны нестабильные фичи
# (build-std, кастомные таргеты, custom_test_frameworks).
# Дата зафиксирована после первой зелёной сборки — ради воспроизводимости.
# Обновляем осознанно, проверяя, что сборка остаётся зелёной.
[toolchain]
channel = "nightly-2026-06-26"
components = ["rust-src", "llvm-tools-preview"]
